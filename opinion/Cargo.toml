[package]
name = "opinion"
version = "0.1.0"
edition = "2024"


[dependencies]
bevy = "0.17.2"

# This dependency is used only when NOT compiling for wasm32
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
rdev = "0.5.3"

